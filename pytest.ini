[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that run actual WOFOST simulations (slow, ~2-5s each)
# Show short tracebacks for clean output
addopts = -v --tb=short
